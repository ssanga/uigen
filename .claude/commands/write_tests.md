Write comprehensive test for : $ARGUMENTS

Testing conventions:
* Use Vitest with React Testing Library
* Place  tests in a `__tests__` directory adjacent to the component being tested
* Name test files with the format `ComponentName.test.tsx`
* Use @/ prefix for imports to avoid relative paths

Coverage:
* Test happy paths
* Test edge cases and error handling